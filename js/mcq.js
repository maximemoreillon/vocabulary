function toggle_reading_visibility(){
  var reading = document.getElementById("reading");
  if(reading.style.visibility === "visible") reading.style.visibility = "hidden";
  else reading.style.visibility = "visible";
}
